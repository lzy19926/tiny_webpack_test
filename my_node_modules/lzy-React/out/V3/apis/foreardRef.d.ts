/*******************************************************************************************
 * 将element包裹一层,作为FORWARD_REF_TYPE类型节点
 * 于markRef的时候记录ref
 * 在render阶段创建fiber节点时(renderElement)， 将该类型节点的render函数取出执行,注入ref
 * 而普通的函数组件,markRef的时候会阻止ref的传递, (由于ref只能绑定给dom节点)
 * 这个API只做了一件事,将组件函数包装为FORWARD_REF_TYPE,后续按照forwardRef处理
 * *****************************************************************************************/
import { ElementNode } from '../myReactCore/Interface';
export declare function forwardRef(render: (props: any, ref: any) => ElementNode): {
    $$typeof: symbol;
    render: (props: any, ref: any) => ElementNode;
};
