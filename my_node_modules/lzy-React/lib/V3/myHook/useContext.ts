import { ReactContext } from '../apis/createContext'
export { ReactContext }

//! 总结:useContext直接返回当前dispatcher中的context对象
// 返回current
export function mountContext(Context: ReactContext) {
  return Context._currentValue
}
