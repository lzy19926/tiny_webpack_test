import LzyReact from '../../../../lzy-React/out/index_V3'

export default function Button(props, children) {

    const { type, onClick } = props
    const btnClassName = `btn btn-${type.toLowerCase()}`;

    return (
        <button type="button" class={btnClassName} onClick={onClick}>{children}</button>
    )
}