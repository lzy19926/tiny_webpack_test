import LzyReact from '../../../../lzy-React/out/index_V3'
import './index.css'

export default function TabContainer(props, children) {

    const { dataSorce, style = '' } = props

    return (
        <div style={style}>
            <div class="d-flex align-items-start lzyTab">
                <div class="nav flex-column nav-pills me-5 lzyTab-left" id="v-pills-tab" role="tablist" aria-orientation="vertical">

                    {dataSorce.map(({ title, key }, index) => (
                        <button
                            class={index === 0 ? "nav-link active" : "nav-link"}
                            id={key + "tab"}
                            data-bs-toggle="pill"
                            data-bs-target={"#" + key}
                            type="button"
                            role="tab"
                            aria-controls={key}
                            aria-selected="true">
                            {title}
                        </button>
                    ))}


                </div>


                <div class="tab-content" id="v-pills-tabContent">
                    {dataSorce.map(({ key, render }, index) => (
                        <div
                            class={index === 0 ? "tab-pane fade show active" : "tab-pane fade"}
                            id={key}
                            role="tabpanel"
                            aria-labelledby={key + "tab"}>
                            {render()}
                        </div>
                    ))}
                </div>


            </div>
        </div>

    )
}





