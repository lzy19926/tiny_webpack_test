import LzyReact from '../../../../lzy-React/out/index_V3'

export default function Block(props) {
    const { line = true, size = 50 } = props

    const mainStyle = `
    margin:${size}px 10px;
    ${line ? 'border-top:1px solid #ccc' : ''}
    `
    return <div style={mainStyle}></div>
}