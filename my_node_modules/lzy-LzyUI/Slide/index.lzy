
import LzyReact from 'lzy-react'
import './index.css'
export default function SideSlide(props, children) {

    //todo 配置方向  现在只提供右侧出
    const { id } = props

    return (
        <div>
            <div class="modal fade" id={id}>
                <div class="modal-dialog lzy-side-slide-right slide-left">
                    {children}
                </div>
            </div>
        </div>
    );
}