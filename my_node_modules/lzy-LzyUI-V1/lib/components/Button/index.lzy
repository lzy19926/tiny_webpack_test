import LzyReact from 'lzy-react'

export default function Button(props, children) {

    const { type, onClick, onClickCapture } = props
    const btnClassName = `btn btn-${type.toLowerCase()}`;

    return (
        <button type="button" class={btnClassName} onClick={onClick} onClickCapture={onClickCapture}>{children}</button>
    )
}