
import * as LzyReact from '../../../my_node_modules/lzy-React/out/index_V2' // 因为export default的功能不完善 需要这样进行引入
import { myUseState, render } from '../../../my_node_modules/lzy-React/out/index_V2' // 需要引入

//! Demo组件
function Test(props) {

    const [arr, setArr] = myUseState([1, 2, 3])
    console.log(arr);
    return (
        <LZY-TEMPLATE>
            <button onClick={() => { setArr([...arr, 0]) }}>添加</button>
            <button onClick={() => { setArr(arr.splice(1)) }}>删除</button>
            {arr.map((item, index) => <div key={index}>{item}</div>)}
        </LZY-TEMPLATE>
    )
}


render(<Test />, document.getElementById('root'))

